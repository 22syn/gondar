## אובדן נתונים אמיתי, אתמול בלילה

`fragility_daily` איבד את השורה של **2026-08-10**. 264 שורות → 263, והיום החדש ביותר נמחק.

## מה קרה

שתי הסריקות של אתמול לא הסכימו על טווח הנתונים:

| ריצה | aligned days |
|---|---|
| 20:15 (זמנית) | `2022-01-27..`**`2026-08-10`** — 1121 ימים, 1012 מנוקדים |
| 23:45 (מיושבת) | `2022-01-27..`**`2026-08-07`** — 1120 ימים, 1011 מנוקדים |

היישור הוא **חיתוך על פני כל 10 מניות הסל**. מספיק ש-Yahoo מפסיק להחזיר את הנר האחרון של מניה אחת, וכל היום נופל מהסדרה.

ואז ה-ingest עשה:

```sql
DELETE FROM fragility_daily WHERE scan_date >= '2025-08-xx'
```

**בלי חסם עליון.** הוא מחק את 08-10 והכניס בחזרה רק עד 08-07. לשורה הזו לא היה תחליף.

זו אותה מחלקת תקלה כמו התקרית של סוף השבוע (149→43): מחיקה שמניחה שמה שיוכנס מכסה את מה שנמחק.

## התיקון

```diff
- sql: 'DELETE FROM fragility_daily WHERE scan_date >= ?',
- params: [rows[0]!.date],
+ sql: 'DELETE FROM fragility_daily WHERE scan_date >= ? AND scan_date <= ?',
+ params: [rows[0]!.date, rows[rows.length - 1]!.date],
```

מוחקים רק את מה שעומדים לכתוב מחדש. תקלת fetch רגעית הופכת ל-no-op במקום למחיקה קבועה.

התכונה המקורית נשמרת — מחיקת טווח (ולא לפי תאריך בודד) עדיין מנקה שורות שהזזת לוח שנה הייתה מייתמת, פשוט בתוך החלון שנכתב.

## בדיקות

9/9 עוברות, כולל בדיקת רגרסיה חדשה שמוודאת שהמחיקה לא מגיעה מעבר לשורה החדשה ביותר שנכתבת.

## נותר לטיפול נפרד

- אותו באג קיים ב-`main` (ה-Smart שעדיין כותב) — PR נפרד
- שורת 08-10 שאבדה ניתנת לשחזור מארטיפקט הסנפשוט של 21:00, שמכיל את ה-payload המלא

🤖 Generated with [Claude Code](https://claude.com/claude-code)
